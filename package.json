{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start-server": "cd packages/server && yarn run start-server"
  },
  "dependencies": {
    "serve": "^11.3.2"
  }
}
